export default function NotFound() {
  return (
    <div className="starter-shell">
      <div className="starter-card starter-stack">
        <p className="starter-eyebrow">Not Found</p>
        <h1 className="starter-title">CMS content was not found</h1>
        <p className="starter-subtle">
          Check the slug, confirm the CMS record exists, and verify the current
          website ID is pointing at the right site.
        </p>
      </div>
    </div>
  );
}
